type LoginProps = {};

const Login = (props: LoginProps) => {
    return (
        <div
            style={{
                width: '500px',
                height: '500px',
                backgroundColor: 'red',
            }}
        >
            Login
        </div>
    );
};

export default Login;
